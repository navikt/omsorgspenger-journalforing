name: CodeQL
on:
  push:
    branches: main
  workflow_dispatch:

jobs:
  analyze:
    name: Analyze
    uses: navikt/sif-gha-workflows/.github/workflows/codeql-analysis.yml@main
    secrets: inherit
